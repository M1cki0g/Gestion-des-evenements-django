# Essential packages only
Django==3.2.19
gunicorn==20.1.0
dj-database-url==0.5.0
python-dotenv==0.19.2
whitenoise==5.3.0
psycopg2-binary==2.9.5

# Keep only the UI package that's already in use
django-simpleui==2022.12.22

# Image processing - add back after successful deployment
# Pillow==9.5.0
